<?xml version="1.0" encoding="UTF-8"?>

<!-- Do not edit this file, it will be overwritten on install.
        Copy the file to $HOME/.config/openbox/ instead. -->

<openbox_config xmlns="http://openbox.org/3.4/rc"
		xmlns:xi="http://www.w3.org/2001/XInclude">

<resistance>
  <!-- Resistance between two windows before they overlap (pixels). -->
  <strength>10</strength>
  <!-- Resistance between a window and the screen edge (pixels). -->
  <screen_edge_strength>20</screen_edge_strength>
</resistance>

<focus>
  <!-- Give focus to new windows when they are created. -->
  <focusNew>no</focusNew>
  <!-- Window focus follows mouse. -->
  <followMouse>yes</followMouse>
  <!-- Focus last focused window on desktop when switching to it. -->
  <focusLast>yes</focusLast>
  <!-- Focus windows under mouse even when mouse is not moving. -->
  <underMouse>yes</underMouse>
  <!-- Delay before window under mouse takes focus -->
  <focusDelay>300</focusDelay>
  <!-- Raise windows to top when they are focused. -->
  <raiseOnFocus>no</raiseOnFocus>
</focus>

<placement>
  <!-- 'Smart' - windows placed automatically.
       'UnderMouse' - windows placed under mouse. -->
  <policy>Smart</policy>
  <!-- Place windows in the center of free area or in top left corner. -->
  <center>yes</center>
  <!-- With Smart placement, new windows will be placed on:
       'Any' - any monitor
       'Mouse' - where the mouse is
       'Active' - where the active window is
       'Primary' - only on the primary monitor -->
  <monitor>Primary</monitor>
  <!-- The primary monitor where popups should be placed. It can be an index
       from 1, specifying a particular monitor, or:
       'Mouse' - where the mouse is
       'Active' - where the active window is -->
  <primaryMonitor>Mouse</primaryMonitor>
</placement>

<theme>
  <!-- Theme name -->
  <name>Onyx</name>
  <!-- Layout of the title bar. Available characters are NDSLIMC,
       each can occur at most once.
       N: window icon
       L: window label (AKA title).
       I: iconify
       M: maximize
       C: close
       S: shade (roll up/down)
       D: omnipresent (on all desktops). -->
  <titleLayout>NLIMC</titleLayout>
  <!-- Should windows keep the border even when decorations are turned off. -->
  <keepBorder>yes</keepBorder>
  <!-- Animate iconify/minimize action. -->
  <animateIconify>yes</animateIconify>

  <!-- Font to use for each window element.
       place - the element
       name - font name
       size - font size in points
       weight - 'bold' or 'normal'
       slant - 'italic' or 'normal' -->

  <!-- Title bar of active window -->
  <font place="ActiveWindow">
    <name>sans</name>
    <size>10</size>
    <weight>normal</weight>
    <slant>normal</slant>
  </font>
  <!-- Title bar of inactive window -->
  <font place="InactiveWindow">
    <name>sans</name>
    <size>10</size>
    <weight>normal</weight>
    <slant>italic</slant>
  </font>
  <!-- Titles in the menu -->
  <font place="MenuHeader">
    <name>sans</name>
    <size>10</size>
    <weight>bold</weight>
    <slant>normal</slant>
  </font>
  <!-- Items in the menu -->
  <font place="MenuItem">
    <name>sans</name>
    <size>10</size>
    <weight>normal</weight>
    <slant>normal</slant>
  </font>
  <!-- Text in popups -->
  <font place="ActiveOnScreenDisplay">
    <name>sans</name>
    <size>9</size>
    <weight>normal</weight>
    <slant>normal</slant>
  </font>
  <font place="InactiveOnScreenDisplay">
    <name>sans</name>
    <size>9</size>
    <weight>normal</weight>
    <slant>normal</slant>
  </font>
</theme>

<desktops>
  <!-- The number of virtual destops to use -->
  <number>4</number>
  <!-- Desktop to display initially -->
  <firstdesk>1</firstdesk>
  <!-- Optional names for desktops -->
  <names>
    <name>main</name>
    <name>terms</name>
  </names>
  <!-- Time to show desktop popup when switching (ms). -->
  <popupTime>875</popupTime>
</desktops>

<resize>
  <!-- Redraw window contents while resizing -->
  <drawContents>yes</drawContents>
  <!-- Show the move/resize popup:
       'Always'
       'Never'
       'Nonpixel' - show only when resizing windows that specify they are to
                    be resized in increments > 1 pixel (often terminals). -->
  <popupShow>Nonpixel</popupShow>
  <!-- Where to show the move/resize popup:
       'Top' - above window titlebar
       'Center' - centered on window
       'Fixed' - in fixed location, specified by popupFixedPosition -->
  <popupPosition>Center</popupPosition>
  <!-- Location on screen to show move/resize popup. -->
  <popupFixedPosition>
    <!-- x-coordinate:
         positive value - distance from left edge
         negative value - distance from right edge
         'Center' - horizontal center -->
    <x>Center</x>
    <!-- y-coordinate:
         positive value - distance from top edge
         negative value - distance from bottom edge
         'Center' - vertical center -->
    <y>Center</y>
  </popupFixedPosition>
</resize>

<!-- You can reserve a portion of your screen where windows will not cover when
     they are maximized, or when they are initially placed.
     Many programs reserve space automatically, but you can use this in other
     cases. -->
<margins>
  <top>0</top>
  <bottom>0</bottom>
  <left>0</left>
  <right>0</right>
</margins>

<!-- The dock is shown only when one or more docks are running. -->
<dock>
  <!-- Where to show the dock:
       (Top|Bottom)(Left|Right|)|Top|Bottom|Left|Right|Floating -->
  <position>TopLeft</position>
  <!-- Coordinates in pixels when position is set to 'Floating' -->
  <floatingX>0</floatingX>
  <floatingY>0</floatingY>
  <!-- Dock should not set a strut, ie. windows may cover it. -->
  <noStrut>no</noStrut>
  <!-- Window layer of the dock:
       Above|Normal|Below -->
  <stacking>Above</stacking>
  <!-- Direction in which the dock is to be laid out
       Vertical|Horizontal -->
  <direction>Vertical</direction>
  <!-- Hide dock automatically when mouse is not over it -->
  <autoHide>no</autoHide>
  <!-- Delay before dock is hidden when mouse leaves it (ms) -->
  <hideDelay>300</hideDelay>
  <!-- Delay before dock is shown when mouse enters it (ms) -->
  <showDelay>300</showDelay>
  <!-- Button for moving individual dockapps around dock
       Left|Middle|Right -->
  <moveButton>Middle</moveButton>
</dock>

<!-- Key bindings
     S - shift
     C - control
     A - alt
     W - super/windows
     M - meta
     H - hyper -->
<keyboard>
  <!-- Cancel key chain -->
  <chainQuitKey>C-g</chainQuitKey>

  <!-- Keybindings for applications -->
  <!-- Lock screen -->
  <keybind key="C-A-l">
    <action name="Execute"><command>slock</command></action>
  </keybind>
  <!-- Browser -->
  <keybind key="C-A-b">
    <action name="Execute"><command>x-www-browser</command></action>
  </keybind>
  <!-- Terminal -->
  <keybind key="C-A-t">
    <action name="Execute"><command>x-terminal-emulator</command></action>
  </keybind>

  <!-- Keybindings for desktop switching -->
  <keybind key="C-A-Left">
    <action name="GoToDesktop"><to>left</to><wrap>no</wrap></action>
  </keybind>
  <keybind key="C-A-Right">
    <action name="GoToDesktop"><to>right</to><wrap>no</wrap></action>
  </keybind>
  <keybind key="C-A-Up">
    <action name="GoToDesktop"><to>up</to><wrap>no</wrap></action>
  </keybind>
  <keybind key="C-A-Down">
    <action name="GoToDesktop"><to>down</to><wrap>no</wrap></action>
  </keybind>
  <keybind key="S-A-Left">
    <action name="SendToDesktop"><to>left</to><wrap>no</wrap></action>
  </keybind>
  <keybind key="S-A-Right">
    <action name="SendToDesktop"><to>right</to><wrap>no</wrap></action>
  </keybind>
  <keybind key="S-A-Up">
    <action name="SendToDesktop"><to>up</to><wrap>no</wrap></action>
  </keybind>
  <keybind key="S-A-Down">
    <action name="SendToDesktop"><to>down</to><wrap>no</wrap></action>
  </keybind>
  <keybind key="W-F1">
    <action name="GoToDesktop"><to>1</to></action>
  </keybind>
  <keybind key="W-F2">
    <action name="GoToDesktop"><to>2</to></action>
  </keybind>
  <keybind key="W-F3">
    <action name="GoToDesktop"><to>3</to></action>
  </keybind>
  <keybind key="W-F4">
    <action name="GoToDesktop"><to>4</to></action>
  </keybind>
  <keybind key="W-d">
    <action name="ToggleShowDesktop"/>
  </keybind>

  <!-- Keybindings for windows -->
  <keybind key="A-F4">
    <action name="Close"/>
  </keybind>
  <keybind key="A-Escape">
    <action name="Lower"/>
    <action name="FocusToBottom"/>
    <action name="Unfocus"/>
  </keybind>
  <keybind key="A-space">
    <action name="ShowMenu"><menu>client-menu</menu></action>
  </keybind>

  <!-- Keybindings for window switching -->
  <keybind key="A-Tab">
    <action name="NextWindow">
      <finalactions>
        <action name="Focus"/>
        <action name="Raise"/>
        <action name="Unshade"/>
      </finalactions>
    </action>
  </keybind>
  <keybind key="A-S-Tab">
    <action name="PreviousWindow">
      <finalactions>
        <action name="Focus"/>
        <action name="Raise"/>
        <action name="Unshade"/>
      </finalactions>
    </action>
  </keybind>
  <keybind key="C-A-Tab">
    <action name="NextWindow">
      <panels>yes</panels><desktop>yes</desktop>
      <finalactions>
        <action name="Focus"/>
        <action name="Raise"/>
        <action name="Unshade"/>
      </finalactions>
    </action>
  </keybind>

  <!-- Keybindings for window switching with the arrow keys -->
  <keybind key="W-S-Right">
    <action name="DirectionalCycleWindows">
      <direction>right</direction>
    </action>
  </keybind>
  <keybind key="W-S-Left">
    <action name="DirectionalCycleWindows">
      <direction>left</direction>
    </action>
  </keybind>
  <keybind key="W-S-Up">
    <action name="DirectionalCycleWindows">
      <direction>up</direction>
    </action>
  </keybind>
  <keybind key="W-S-Down">
    <action name="DirectionalCycleWindows">
      <direction>down</direction>
    </action>
  </keybind>

  <!-- Take screenshot of current window with scrot -->
  <keybind key="Print">
    <action name="Execute"><command>scrot -u</command></action>
  </keybind>
  <!-- Take screenshot of selection with scrot -->
  <keybind key="C-Print">
    <action name="Execute"><command>scrot -s</command></action>
  </keybind>
  <!-- Take screenshot of entire workspace with scrot -->
  <keybind key="A-Print">
    <action name="Execute"><command>scrot</command></action>
  </keybind>
  <!-- Take screenshot of attached Android device with adb -->
  <keybind key="W-Print">
    <action name="Execute"><command>adb screencap</command></action>
  </keybind>
</keyboard>

<mouse>
  <!-- Number of pixels the mouse must move before a drag begins -->
  <dragThreshold>1</dragThreshold>
  <!-- Time between two clicks to register as a double-click (ms) -->
  <doubleClickTime>200</doubleClickTime>
  <!-- Time before changing desktops when the pointer touches the edge of the
       screen while moving a window (ms). Set to 0 to disable warping -->
  <screenEdgeWarpTime>400</screenEdgeWarpTime>
  <!-- Set to true to warp mouse to next desktop when touching the edge
       of the screen. -->
  <screenEdgeWarpMouse>false</screenEdgeWarpMouse>

  <!-- Mouse bindings for different contexts -->
  <!-- Frame - entire window frame -->
  <context name="Frame">
    <mousebind button="A-Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
    </mousebind>
    <mousebind button="A-Left" action="Click">
      <action name="Unshade"/>
    </mousebind>
    <mousebind button="A-Left" action="Drag">
      <action name="Move"/>
    </mousebind>

    <mousebind button="A-Right" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="Unshade"/>
    </mousebind>
    <mousebind button="A-Right" action="Drag">
      <action name="Resize"/>
    </mousebind> 

    <mousebind button="A-Middle" action="Press">
      <action name="Lower"/>
      <action name="FocusToBottom"/>
      <action name="Unfocus"/>
    </mousebind>

    <mousebind button="A-Up" action="Click">
      <action name="GoToDesktop"><to>previous</to></action>
    </mousebind>
    <mousebind button="A-Down" action="Click">
      <action name="GoToDesktop"><to>next</to></action>
    </mousebind>
    <mousebind button="C-A-Up" action="Click">
      <action name="GoToDesktop"><to>previous</to></action>
    </mousebind>
    <mousebind button="C-A-Down" action="Click">
      <action name="GoToDesktop"><to>next</to></action>
    </mousebind>
    <mousebind button="A-S-Up" action="Click">
      <action name="SendToDesktop"><to>previous</to></action>
    </mousebind>
    <mousebind button="A-S-Down" action="Click">
      <action name="SendToDesktop"><to>next</to></action>
    </mousebind>

    <mousebind button="W-Right" action="Press">
      <action name="ShowMenu"><menu>root-menu</menu></action>
    </mousebind>
  </context>

  <!-- Titlebar - the window titlebar -->
  <context name="Titlebar">
    <mousebind button="Left" action="Drag">
      <action name="Move"/>
    </mousebind>
    <mousebind button="Left" action="DoubleClick">
      <action name="ToggleMaximize"/>
    </mousebind>

    <mousebind button="Up" action="Click">
      <action name="if">
        <shaded>no</shaded>
        <then>
          <action name="Shade"/>
          <action name="FocusToBottom"/>
          <action name="Unfocus"/>
          <action name="Lower"/>
        </then>
      </action>
    </mousebind>
    <mousebind button="Down" action="Click">
      <action name="if">
        <shaded>yes</shaded>
        <then>
          <action name="Unshade"/>
          <action name="Raise"/>
        </then>
      </action>
    </mousebind>
  </context>

  <!-- Corners and edges of window -->
  <context name="Titlebar Top Right Bottom Left TLCorner TRCorner BRCorner BLCorner">
    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="Unshade"/>
    </mousebind>

    <mousebind button="Middle" action="Press">
      <action name="Lower"/>
      <action name="FocusToBottom"/>
      <action name="Unfocus"/>
    </mousebind>

    <mousebind button="Right" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="ShowMenu"><menu>client-menu</menu></action>
    </mousebind>
  </context>

  <context name="Top">
    <mousebind button="Left" action="Drag">
      <action name="Resize"><edge>top</edge></action>
    </mousebind>
  </context>

  <context name="Left">
    <mousebind button="Left" action="Drag">
      <action name="Resize"><edge>left</edge></action>
    </mousebind>
  </context>

  <context name="Right">
    <mousebind button="Left" action="Drag">
      <action name="Resize"><edge>right</edge></action>
    </mousebind>
  </context>

  <context name="Bottom">
    <mousebind button="Left" action="Drag">
      <action name="Resize"><edge>bottom</edge></action>
    </mousebind>

    <mousebind button="Right" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="ShowMenu"><menu>client-menu</menu></action>
    </mousebind>
  </context>

  <context name="TRCorner BRCorner TLCorner BLCorner">
    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="Unshade"/>
    </mousebind>
    <mousebind button="Left" action="Drag">
      <action name="Resize"/>
    </mousebind>
  </context>

  <!-- The window inside the decorations -->
  <context name="Client">
    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
    </mousebind>
    <mousebind button="Middle" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
    </mousebind>
    <mousebind button="Right" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
    </mousebind>
  </context>

  <!-- The window icon in the titlebar -->
  <context name="Icon">
    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="Unshade"/>
      <action name="ShowMenu"><menu>client-menu</menu></action>
    </mousebind>
    <mousebind button="Right" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="ShowMenu"><menu>client-menu</menu></action>
    </mousebind>
  </context>

  <!-- The All-desktops/Omnipresent button in the window titlebar -->
  <context name="AllDesktops">
    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="Unshade"/>
    </mousebind>
    <mousebind button="Left" action="Click">
      <action name="ToggleOmnipresent"/>
    </mousebind>
  </context>

  <!-- The Shade button in the window titlebar -->
  <context name="Shade">
    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
    </mousebind>
    <mousebind button="Left" action="Click">
      <action name="ToggleShade"/>
    </mousebind>
  </context>

  <!-- The Iconify button in the window titlebar -->
  <context name="Iconify">
    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
    </mousebind>
    <mousebind button="Left" action="Click">
      <action name="Iconify"/>
    </mousebind>
  </context>

  <!-- The Maximize button in the window titlebar -->
  <context name="Maximize">
    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="Unshade"/>
    </mousebind>
    <mousebind button="Middle" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="Unshade"/>
    </mousebind>
    <mousebind button="Right" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="Unshade"/>
    </mousebind>
    <mousebind button="Left" action="Click">
      <action name="ToggleMaximize"/>
    </mousebind>
    <mousebind button="Middle" action="Click">
      <action name="ToggleMaximize"><direction>vertical</direction></action>
    </mousebind>
    <mousebind button="Right" action="Click">
      <action name="ToggleMaximize"><direction>horizontal</direction></action>
    </mousebind>
  </context>

  <!-- The Close button in the window titlebar -->
  <context name="Close">
    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
      <action name="Unshade"/>
    </mousebind>
    <mousebind button="Left" action="Click">
      <action name="Close"/>
    </mousebind>
  </context>

  <!-- The desktop or background (root window) -->
  <context name="Desktop">
    <mousebind button="Up" action="Click">
      <action name="GoToDesktop"><to>previous</to></action>
    </mousebind>
    <mousebind button="Down" action="Click">
      <action name="GoToDesktop"><to>next</to></action>
    </mousebind>

    <mousebind button="A-Up" action="Click">
      <action name="GoToDesktop"><to>previous</to></action>
    </mousebind>
    <mousebind button="A-Down" action="Click">
      <action name="GoToDesktop"><to>next</to></action>
    </mousebind>
    <mousebind button="C-A-Up" action="Click">
      <action name="GoToDesktop"><to>previous</to></action>
    </mousebind>
    <mousebind button="C-A-Down" action="Click">
      <action name="GoToDesktop"><to>next</to></action>
    </mousebind>

    <mousebind button="Left" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
    </mousebind>
    <mousebind button="Right" action="Press">
      <action name="Focus"/>
      <action name="Raise"/>
    </mousebind>
  </context>

  <!-- Similar to Desktop, used for Root menus -->
  <context name="Root">
    <!-- Menus -->
    <mousebind button="Middle" action="Press">
      <action name="ShowMenu"><menu>client-list-combined-menu</menu></action>
    </mousebind> 
    <mousebind button="Right" action="Press">
      <action name="ShowMenu"><menu>root-menu</menu></action>
    </mousebind>
  </context>

  <!-- Bindings in this context are available while a window is being moved
       or resized interactively -->
  <context name="MoveResize">
    <mousebind button="Up" action="Click">
      <action name="GoToDesktop"><to>previous</to></action>
    </mousebind>
    <mousebind button="Down" action="Click">
      <action name="GoToDesktop"><to>next</to></action>
    </mousebind>
    <mousebind button="A-Up" action="Click">
      <action name="GoToDesktop"><to>previous</to></action>
    </mousebind>
    <mousebind button="A-Down" action="Click">
      <action name="GoToDesktop"><to>next</to></action>
    </mousebind>
  </context>
</mouse>

<menu>
  <!-- Specify menu files with unique menu Ids. -->
  <!-- Default menu file (or custom one in $HOME/.config/openbox/) -->
  <file>menu.xml</file>
  <!-- Delay clicking and holding before menu will automatically hide when
       released (ms). Holding for a shorter time will not hide the menu -->
  <hideDelay>200</hideDelay>
  <!-- Center submenus vertically about the parent entry -->
  <middle>no</middle>
  <!-- Delay before showing a submenu on hover (ms). A negative value will
       require the submenu to be clicked to show it. -->
  <submenuShowDelay>100</submenuShowDelay>
  <!-- Delay before hiding a submenu when selecting another entry in the parent
       menu. A negative value will require another submenu to be opened before
       this one is hidden. -->
  <submenuHideDelay>400</submenuHideDelay>
  <!-- Show window icons in Desktop and Windows menus 
       (client-list-{combined-}menu) -->
  <applicationIcons>yes</applicationIcons>
  <!-- Show manage desktops section in Desktop and Windows menus -->
  <manageDesktops>no</manageDesktops>
</menu>

<applications>
<!--
  # this is an example with comments through out. use these to make your
  # own rules, but without the comments of course.
  # you may use one or more of the name/class/role/title/type rules to specify
  # windows to match

  <application name="the window's _OB_APP_NAME property (see obxprop)"
              class="the window's _OB_APP_CLASS property (see obxprop)"
               role="the window's _OB_APP_ROLE property (see obxprop)"
              title="the window's _OB_APP_TITLE property (see obxprop)"
               type="the window's _OB_APP_TYPE property (see obxprob)..
                      (if unspecified, then it is 'dialog' for child windows)">
  # you may set only one of name/class/role/title/type, or you may use more
  # than one together to restrict your matches.

  # the name, class, role, and title use simple wildcard matching such as those
  # used by a shell. you can use * to match any characters and ? to match
  # any single character.

  # the type is one of: normal, dialog, splash, utility, menu, toolbar, dock,
  #    or desktop

  # when multiple rules match a window, they will all be applied, in the
  # order that they appear in this list


    # each rule element can be left out or set to 'default' to specify to not 
    # change that attribute of the window

    <decor>yes</decor>
    # enable or disable window decorations

    <shade>no</shade>
    # make the window shaded when it appears, or not

    <position force="no">
      # the position is only used if both an x and y coordinate are provided
      # (and not set to 'default')
      # when force is "yes", then the window will be placed here even if it
      # says you want it placed elsewhere.  this is to override buggy
      # applications who refuse to behave
      <x>center</x>
      # a number like 50, or 'center' to center on screen. use a negative number
      # to start from the right (or bottom for <y>), ie -50 is 50 pixels from the
      # right edge (or bottom).
      <y>200</y>
      <monitor>1</monitor>
      # specifies the monitor in a xinerama setup.
      # 1 is the first head, or 'mouse' for wherever the mouse is
    </position>

    <focus>yes</focus>
    # if the window should try be given focus when it appears. if this is set
    # to yes it doesn't guarantee the window will be given focus. some
    # restrictions may apply, but Openbox will try to

    <desktop>1</desktop>
    # 1 is the first desktop, 'all' for all desktops

    <layer>normal</layer>
    # 'above', 'normal', or 'below'

    <iconic>no</iconic>
    # make the window iconified when it appears, or not

    <skip_pager>no</skip_pager>
    # asks to not be shown in pagers

    <skip_taskbar>no</skip_taskbar>
    # asks to not be shown in taskbars. window cycling actions will also
    # skip past such windows

    <fullscreen>yes</fullscreen>
    # make the window in fullscreen mode when it appears

    <maximized>true</maximized>
    # 'Horizontal', 'Vertical' or boolean (yes/no)
  </application>

  # end of the example
-->
</applications>

</openbox_config>
