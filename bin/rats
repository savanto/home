#!/usr/bin/env python

import sys

if len(sys.argv) != 6:
  print "Usage: rats #5 #4 #3 #2 #1"
  sys.exit(1)

stars = [ int(s) for s in sys.argv[1:6] ]
rats = [ a * b for a, b in zip(stars, xrange(5, 0, -1)) ]
tot = sum(stars)
avg = sum(rats) / float(tot)
print "%s in %s ratings" % (avg, tot)

